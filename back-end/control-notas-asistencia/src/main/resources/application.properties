# Configuración de la base de datos MySQL
spring.datasource.url=jdbc:mysql://localhost:3307/pruebaproyecto
spring.datasource.username=root
spring.datasource.password=Yessamin1
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de HikariCP
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.leak-detection-threshold=2000

# Configuración de JPA con Hibernate
spring.jpa.hibernate.ddl-auto=update  # Para crear y actualizar automáticamente las tablas de la base de datos
spring.jpa.show-sql=true  
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect  # Ajuste del dialecto a MySQL
spring.jpa.generate-ddl=true

